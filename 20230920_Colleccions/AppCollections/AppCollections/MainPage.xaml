<Page
    x:Class="AppCollections.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppCollections"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Loaded="Page_Loaded"
    >

    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox
            AcceptsReturn="True"
            x:Name="txbDebug"
            Grid.Column="0" Grid.Row="0" Grid.RowSpan="3" ></TextBox>
        <TextBox x:Name="txbNew" Grid.Column="1" Grid.Row="0"></TextBox>
        <ListBox x:Name="lsb1" Grid.Column="1" Grid.Row="1"></ListBox>
        <ListBox x:Name="lsb2"  Grid.Column="1" Grid.Row="2"></ListBox>
        
    </Grid>
</Page>
